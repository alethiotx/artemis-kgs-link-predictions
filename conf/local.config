params {
    dataset = 'hetionet'
    embedding = 'ComplEx'
    outdir = 'output'
    env = 'test'
    // whether to downsample to 10000 terms
    sample = false
}
process.errorStrategy = 'terminate'
docker.enabled = false