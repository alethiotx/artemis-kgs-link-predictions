params {
    dataset = 'hetionet'
    model = 's3://alethiotx-artemis/data/kgs/embeddings/' + params.dataset + '/trained_model.pkl'
    outdir = 'output'
    env = 'test'
    // whether to downsample to 10000 terms
    sample = false
}
process.errorStrategy = 'terminate'
docker.enabled = false